{
    "name": "ethica",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "server",
        "front"
    ],
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@nestjs/schedule": "^6.1.3",
        "firebase-admin": "^14.2.0"
    }
}
